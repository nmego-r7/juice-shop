# Docker'
# Build a Docker image
# https://docs.microsoft.com/azure/devops/pipelines/languages/docker

trigger:
- master

resources:
- repo: self

variables:
  tag: '$(Build.BuildId)'

stages:
- stage: Build
  displayName: Build image
  jobs:
  - job: Build
    displayName: Build
    pool:
      vmImage: ubuntu-latest
    steps:
    - task: rapid7-insightappsec@1
      inputs:
        apiConnection: 'Natalie_AnonymooseAzureUserKey'
        application: 'a6dda79b-6525-4600-9047-4fec7b8e94ea'
        scanConfig: 'CICD Hackazon Scan Config - Unauthenticated'
        waitForCompletion: true
        generateFindingsReport: true
        publishPipelineArtifacts: true
        artifactPerReport: true
    